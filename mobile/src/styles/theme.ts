import { Colors } from "./colors";

export const LightTheme = {
  dark: false,
  colors: {
    primary: Colors.PRIMARY,
    background: Colors.WHITE,
    card: Colors.WHITE,
    text: Colors.BLACK,
    border: Colors.LIGHT_PALE,
    notification: Colors.PRIMARY
  }
};
